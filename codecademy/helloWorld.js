//This is known as a JSX element.
const h1 = <h1>Hello world</h1>;

    const article = <article>This is an article.</article>;

const p1 = <p id="large">foo</p>;

    const p2=<p id="small">bar</p>;

    //If a JSX expression takes up more than one line, then you must wrap the entire expression in (brackets).

const example =
    (
    <a href="https://www.example.com">
    <h1>
        Click me!
    </h1>
</a>
)

const myDiv = (<div>
    <h1>
        Hello world
    </h1>
</div>
);











