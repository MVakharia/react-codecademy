//In HTML, event listener names are all lowercase.

//In JSX, event listener names are all camelCase.

//You cannot inject an 'if' statement into a JSX expression
// (inside the curly braces between the opening and closing HTML tags)

function displayFact(e) {

}